
alias cp='cp -i'
alias rm='rm -i'
alias rrf='rm -rf'
alias rf='rm -f'
alias mv='mv -i'

alias ll='ls -laF'
alias j='jobs -l'

alias p='~/bin/pause'
# Right now, the function pass (which I kind of like), clobbers this.
alias pd='/usr/local/bin/pass'

alias f.='find .'
alias fn='find . -name'

